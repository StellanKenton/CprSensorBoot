{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "J-Link Debug (CprSensorBoot)",
      "type": "cortex-debug",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "servertype": "jlink",
      "serverpath": "${config:cpr.tools.jlinkGdbServer}",
      "device": "${config:cpr.target.device}",
      "interface": "${config:cpr.target.interface}",
      "executable": "${workspaceFolder}/MDK-ARM/CprSensorBoot/CprSensorBoot.axf",
      "gdbPath": "${config:cpr.tools.armGdbPath}",
      "runToEntryPoint": "main",
      "preLaunchTask": "Keil: Build",
      "serverArgs": [
        "-speed",
        "${config:cpr.target.speed}",
        "-port",
        "${config:cpr.target.gdbPort}",
        "-swoport",
        "${config:cpr.target.swoPort}",
        "-telnetport",
        "${config:cpr.target.telnetPort}"
      ],
      "postLaunchCommands": [
        "monitor reset",
        "monitor halt"
      ],
      "rttConfig": {
        "enabled": true,
        "address": "auto",
        "clearSearch": false,
        "decoders": [
          {
            "label": "RTT",
            "port": 0,
            "encoding": "utf8"
          }
        ]
      }
    }
  ]
}
